package com.aitranslatebubble.data.local

/**
 * Supported target languages for translation.
  *
   * [code] follows ISO 639-1 — the same convention used by ML Kit Translate and most translation
    * APIs — so you can pass it straight through once you wire up a translation backend.
     */